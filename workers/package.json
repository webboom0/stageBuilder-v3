{
  "name": "stagebuilder-v4-api",
  "private": true,
  "version": "1.0.0",
  "description": "StageBuilder v4 API — Cloudflare Workers + R2 (Pages test site)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
