# Cloudflare Pages — git deploy (Workers & Pages build)
# Project name must match the Pages project in the dashboard.
name = "stagebuilder-v3"
compatibility_date = "2024-01-01"
pages_build_output_dir = "pages-dist"
