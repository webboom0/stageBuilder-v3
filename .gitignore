# Node
node_modules/
npm-debug.log*

# Env
.env
.env.*

# OS / IDE
.DS_Store
Thumbs.db
.idea
.cursor/

# Logs
*.log

# Uploaded / local media (keep folder markers)
server/files/fbx/**
server/files/music/**
server/files/video/**
server/files/projects/**
!server/files/**/.gitkeep

# Stage assets linked from v3 (large; local junction)
server/files/stage/

# Local deploy staging (FBX copied here; served via GitHub Release on Render)
deploy/stage/*.fbx

# Three.js runtime junction → pivot html/stageBuilder (local dev)
/runtime

# Cloudflare Pages build output
pages-dist/
.pages-three-cache/

# GitHub 100MB limit / local-only large assets (if present under root files/)
files/fbx/2.fbx
files/fbx/1.fbx
files/fbx/Orc Idle.fbx
